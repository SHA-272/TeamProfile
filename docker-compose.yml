version: '3'

services:
  team_profile:
    build: .
    volumes:
      - ./static:/static
      - ./templates:/templates
      - ./data.db:/data.db
    restart: unless-stopped
    networks:
      ctfnet:
        ipv4_address: 10.0.0.20

networks:
  ctfnet:
    external: true
